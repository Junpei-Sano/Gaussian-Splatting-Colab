# 動画を複数枚の画像にする

- 動画ファイルを、3DGSで使うための画像ファイルにします
- 指定した秒数ごとのフレームを出力します
- 実行方法
    - `python .\video2image.py 入力動画ファイル名 秒数`
    - outputフォルダ内のサブフォルダに画像が出力されます
- ローカルで実行した場合、出力された画像ファイルは Colab 上に手動でコピーする必要があります
